#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;

void solve(){
    ll n, c, v, mod = 1e9+7, cnt = 0;
    cin >> n >> c >> v;
}

int main(){
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  // int t;cin>>t;while(t--)
  solve();
  return 0;
}
